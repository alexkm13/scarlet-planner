# Server
HOST=0.0.0.0
PORT=8000
RELOAD=false

# CORS (comma-separated origins, or * for allow all - dev only)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Optional: path to courses JSON (default: src/data/courses.json)
# COURSES_JSON_PATH=./data/courses.json

# Scraper: session cookies (set one of these for bu-scrape)
# BU_COOKIES_JSON={"JSESSIONID":"...","lcsrftoken":"..."}
# BU_COOKIES_FILE=./cookies.json
